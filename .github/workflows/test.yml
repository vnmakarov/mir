name: test

on: [push]

jobs:
  test:

    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    
    steps:
    - name: update
      run: sudo apt update -y
    - uses: actions/checkout@v1
    - name: make test
      run: make test
